#include "includer.h"
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(int argc, char *argv[])
{
    if(strcmp(argv[1], "config")==0 && strcmp(argv[2], "-s")==0)
    {
        if(argc != 4){
            
        }
    }
}